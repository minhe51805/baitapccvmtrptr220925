# Requirements cho ứng dụng UI đăng nhập Python
# Chạy: pip install -r requirements.txt

# Tkinter đã có sẵn trong Python, không cần cài đặt thêm
# Các thư viện bổ sung (tùy chọn để mở rộng ứng dụng)

# Để làm việc với database
sqlite3  # Có sẵn trong Python

# Để mã hóa mật khẩu (khuyến nghị)
bcrypt>=4.0.0

# Để xử lý file cấu hình
configparser  # Có sẵn trong Python

# Để xử lý thời gian
datetime  # Có sẵn trong Python

# Để tạo GUI đẹp hơn (tùy chọn thay thế tkinter)
# customtkinter>=5.2.0

# Để xử lý JSON
json  # Có sẵn trong Python

# Để validate dữ liệu
re  # Có sẵn trong Python

# Để tạo hash
hashlib  # Có sẵn trong Python

# Ghi chú:
# - Tkinter đã được tích hợp sẵn trong Python 3.x
# - Hầu hết các thư viện cần thiết đã có sẵn trong Python
# - Chỉ cần cài bcrypt nếu muốn mã hóa mật khẩu
# - customtkinter để tạo giao diện đẹp hơn (tùy chọn)